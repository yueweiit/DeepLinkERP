[project]
name = "mobile_operations"
authors = [{ name = "yuewei", email = "akivision@example.com" }]
description = "Mobile warehouse and manufacturing operations for ERPNext"
requires-python = ">=3.14"
readme = "README.md"
dynamic = ["version"]
dependencies = []

[build-system]
requires = ["flit_core >=3.4,<4"]
build-backend = "flit_core.buildapi"

[tool.ruff]
line-length = 110
target-version = "py314"
