# 工作台待处理／成本核对实施记录

目标：按当前有效分摊结果分组；暂估与缺凭证仅提示，不放宽最终确认规则。

- [x] 从生产 e87011f050 创建隔离分支，基线 1065 项测试通过。
- [x] 复现并修复显式日期被 include_history 覆盖，以及权限过滤来源字段导致的误报。
- [x] 实现当前版本／输入哈希／分摊完整性核对，批量加载且不写业务数据。
- [x] 增加待核对／已核对 URL 筛选、相关统计和操作提示，防请求竞态。
- [ ] 规格复核、代码复核、完整测试、资源构建和语法检查。
- [ ] 通过现有 GitHub Actions 发布；线上只读核验分组数量、权限及无写入。

默认待处理与待核对互斥；已核对历史可查询，ERP 队列和最终确认规则保持。
原始来源 JSON 仅供服务器判断，不返回浏览器；所有内部批量查询限定于先取得的用户可读批次 ID。
此次不自动重算、修改金额、确认成本或推送 ERP，不修改密钥及连接配置。

## 发布前证据

- 完整测试 1153 passed；Python 编译、两份生成 JS 语法和 diff 空白检查通过。
- 前端／查询规格复核及质量复核通过；后端规格复核通过。最终全量代码质量复核仍待完成。
- 资源由 build_workbench_assets.py 生成，JS SHA-256 为 2bec373ca4d254dd1c6ff6c0cad16b8c428076faefbdbd660f3a23a6c53fb96b。
- 最终复核追加两项回归：列表试算成功重读分组和统计；列表／统计的只读来源解析禁止触发 AI HTTP，导入同步默认能力不变。两项均先复现失败后通过。
- 生产只读事务内加载新模块到独立进程内存，不部署文件、不修改业务数据：管理员与当前财务用户结果一致。
- 2026-08-10 至 2026-09-08：12 待处理、0 待核对、0 已核对、交集 0。阻断统计可重叠：12 需更新结果，10 缺费用，2 缺采购货值，1 采购来源失效，1 无保存结果，1 旧快照，1 分摊分母缺失。
- 2026-01-01 至 2026-09-08：133 待处理、0 待核对、0 已核对。费用缺失 130，采购货值缺失 100；81 无保存快照、41 旧快照，其余 11 当前哈希不一致，均需重新试算，未自动处理。
- 日期范围内关键词 FL000429 仅返回真实所属批次 vunn2lvq4k，列表与统计范围一致。
- jigl9b07is 的保存快照来自 12:00，早于 12:25 的费用元数据规则变更；正式计算器自身也得到不同输入哈希，不能以金额相同冒充有效结果。其当前输入仅在内存构造新保存结果时能够进入 ready：20 行毛重均为 0、总额 41941.38、暂估及缺凭证仅警告；生产并未重新保存。
- 发布前数据库备份：20260908_134759-deeplinkerp_com-database.sql.gz（31.2 MiB），保留在生产私有备份目录。
- 发布前业务表指纹留待部署后逐项比较：Batch 133、Version 133、Item 1200、Allocation Rule 57。
