# 全运输方式费用统一修正

## 已批准规则

- EXPRESS：国际快递费、快递附加费、清关费、进口税费、当地快递费。
- AIR / SEA：相应国际空运费 / 国际海运费、清关费、进口税费、当地配送费。取消默认空运附加费、海运港杂/货代附加费。
- 全部运输方式清关、进口税费、当地配送默认 MXN、墨西哥补录；配送暂估 0，清关税费空白；快递附加暂估 0 RMB。已保存金额币种保持原样。
- OA 主运费采用统一逻辑标识，保留 oa_logistics_freight 来源编码；明确金额（包括明示零）ESTIMATED，不误推断为 ACTUAL。历史通用名称映射到主运费。
- 人工实际金额、停用费用不可被同步覆盖或恢复；有效主运费冲突必须阻断重复计费，不能默默丢弃。
- 不新增公开 API / 数据库表；报价变化利用现有来源记录保留以供核对；权限和并发守卫保留。

## 任务与验收

- [x] 从最新 production/overseas_costing 隔离工作区，运行基线（881 passed）。
- [x] 测试先行实现共用模板、历史映射、冲突阻断、OA 同步保护及界面状态；独立规格和质量审查。
- [x] 实现幂等线上修复脚本（默认 dry-run），按批次事务、版本/租约/审批有效性检查，完整审计，备份先于 apply。
- [ ] 43 条扫描候选历史 OA 费用：AIR 10 / EXPRESS 29 / SEA 4；重新预检后只改允许修改的当前版本，金额币种原样。
- [ ] 停用 AIR 批次 sjgde0rgc3 (202606151547000071699) 的规则 kr2rgs4kp1，5000 RMB；保留主运费 7000 RMB。
- [ ] 停用 SEA 批次 vunn2lvq4k (202609032107000062462) 的规则 urp34dkllu，3000 RMB；保留主运费 2004 RMB。
- [ ] 截图 AIR 批次 sivocpg6q0，规则 siv8sdf2a1，1416.565 RMB 正确显示国际空运费 / 暂估。
- [ ] 冻结/归档/无效审批/编辑中/冲突批次跳过并列原因；每批次失败整体回滚；已停用旧 OA 规则（包括 20360 RMB）不复活。
- [ ] 受影响批次置 Dirty，不改物料、不自动确认/保存成本结果、不推送 ERP。
- [x] 完整自动测试、构建、Python/JS 语法、diff 检查；独立最终审查。
- [ ] 经 DeepLinkERP GitHub Actions 单一部署（镜像仓库不部署），线上备份、预检、应用修复、只读回归和三种模式浏览器验收。

## 默认约束

DDP 不自动推断包含税费或配送；沿用明确证据/人工确认。任何未知运输方式不默认为某种主运费。现有凭据、公钥、私钥、连接及权限均不变。

基线：464ff664ee354f38418c010ea6a0b4ed46ea5c57。

## 执行记录

- 修复脚本首批 28 项测试先失败后通过；规格复审发现备份完整性、归类后重复、空版本目标遗漏三项边界，新增回归先失败后修正，31 passed，独立规格复审 PASS。
- 部署前站点数据库完整备份已完成：`20260908_120421-deeplinkerp_com-database.sql.gz`，31.2 MiB；gzip 完整性检查通过，SHA-256 `f8abe4b90222a3d6f202136c1453a08d09003f500a57ef10be3c0e51c2d663db`。文件仅保留在服务器现有 private/backups，不进入 Git。
- 修复脚本质量审查 PASS；补充真实保存实现的持久化守卫测试后 42 passed，复审 PASS。金额、币种、物料、版本、费用集合、确认状态异常及审计失败均阻断；两笔停用保留主运费和原始金额。
- 12:16 只读复扫：候选仍为 AIR 10 / EXPRESS 29 / SEA 4；海运目标存在编辑租约，apply 时重新验证，不能绕过。
- 集成测试快照 1027 passed；核心仍在完成最终复审和资源构建。线上业务数据尚未修改。
- 核心规格复审 PASS；补齐显式零报价、人工编辑会话和冻结版本守卫后，全量 1038 passed，前端构建无未提交差异，Python/JS 语法通过。
- 质量审查结合线上只读检查确认数据库为 REPEATABLE-READ；修复脚本改为直接使用锁定查询返回的当前批次、版本、物料和费用，保存后同样以当前读校验。两项回归先失败后修复，44 passed。无需修改数据库配置。
- 核心正在修复同类当前读问题和手工分摊依据保护，质量复审通过前不发布。
- 最终核心与修复脚本质量复审 READY，无未解决 Critical / Important；当前读覆盖人工费用、停用、重复及审批撤销；手工分摊在保存/预览/计算一致，100 元按货值算例正确为 80/20。全量 1049 passed，复审定向 210 passed，包含修复脚本 44 项。
- 已完成构建和 Python/JS 语法校验，计划通过原有 GitHub Actions 发布后执行最新备份、生产预检、修复及只读页面验收。
- 发布前发现生产新增 `c9a3dc41f7` 采购审批来源标记，已无冲突合并保留。合并后全量 1065 passed，构建无差异、Python/JS 语法及差异检查通过。
